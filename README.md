
# Telegram Terabox Bot (Railway Ready)

This bot converts Terabox share links into direct links.

## Setup on Railway

1. Upload these files to your GitHub repo root (no subfolders).
2. On Railway, create a new project from this GitHub repo.
3. In Railway → Variables add:
   BOT_TOKEN = your telegram bot token
4. Deploy. Done 🚀

All files are configured for python-telegram-bot v20+.
